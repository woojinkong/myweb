spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db:3306/konghome?serverTimezone=Asia/Seoul&characterEncoding=UTF-8

    username: konghome
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: dodejqn6@gmail.com
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

konghome:
  jwt:
    secret: ${SECRET}
    access-exp-min: 30
    refresh-exp-days: 7
  frontend-url: "http://43.203.21.175"
  cors:
    allowed-origins:
      - "http://43.203.21.175"

file:
  upload-dir: /home/ubuntu/app/uploads


server:
  port: 8080
